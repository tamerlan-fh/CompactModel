<Window x:Class="CompactModel.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CompactModel.ViewModels"
        mc:Ignorable="d"
        Title="Компактная модель" Height="650" Width="1150" WindowStartupLocation="CenterScreen" Icon="/CompactModel;component/Icons/usatu.ico">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <ResourceDictionary Source="MainWindowResources.xaml" />
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TabControl Grid.Row="1"
                    IsSynchronizedWithCurrentItem="True" 
                    ItemsSource="{Binding Workspaces}"
                    ItemTemplate="{StaticResource TabItemTemplate}"
                    Margin="4" 
                    Background="White"/>
    </Grid>
</Window>
